- name: simple test playbook
  hosts: localhost
  tasks:

    - name: install sshd
      pacman:
        name: openssh
        state: present
        update_cache: yes

    - name: start sshd
      service:
        name: sshd
        state: started
        use: service
