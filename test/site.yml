- name: simple test playbook
  hosts: localhost
  tasks:

    - name: install sshd
      pacman:
        name: openssh-server
        state: present
        update_cache: yes

    - name: start sshd
      service:
        name: ssh
        state: started
