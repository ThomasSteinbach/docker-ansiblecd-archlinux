- name: simple test playbook
  hosts: localhost
  become: true
  tasks:

    - name: install docker
      pacman:
        name: docker
        state: present
        update_cache: yes

    - name: start docker
      service:
        name: docker
        state: started
        use: service
